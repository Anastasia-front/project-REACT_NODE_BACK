const isValidId = require("./isValidId");
const validateBody = require("./validateBody");
const isAutorized = require('./isAutorized')

module.exports = {
  isValidId,
  validateBody,
  isAutorized,
};
